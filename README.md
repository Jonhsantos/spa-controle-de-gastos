# spa-controle-de-gastos
Aplicação web simples para registrar entradas e despesas, acompanhar o saldo e alternar entre modo claro e escuro. Todo o estado fica em memória (não usa banco de dados nem localStorage), então os dados somem ao recarregar a página.
